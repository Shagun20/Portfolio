// src/features/counter/counterSlice.js
import { createSlice } from '@reduxjs/toolkit';

const activeTabSlice= createSlice({
  name: 'activeTab',
  initialState: { value: 0 },
  reducers: {
    
  },
});

export const {  } = activeTabSlice.actions;
export default counterSlice.reducer;